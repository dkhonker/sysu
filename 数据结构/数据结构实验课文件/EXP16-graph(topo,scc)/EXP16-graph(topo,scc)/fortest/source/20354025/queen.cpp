#include<stdio.h>

const int maxn=10;
int n,T;
bool col[max+1],dplus[maxn*2+1],dminus[maxn*2+1];

void queen(int row)
{
	for(int i=1;i<=n;i++)
	{
		if(col[i])
	}
}

int main()
{
	int n;
	scanf("%d",&n);
} 
