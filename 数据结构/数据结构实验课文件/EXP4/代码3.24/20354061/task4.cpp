#include <stdio.h>
#include <stdlib.h>

typedef struct list{
	char w;
	struct list *next;
}LinkList;
int main()
{
	int i=0,m=0,n=0;
	scanf("%d %d",&m,&n);
	LinkList *head,*node,*end;
	head=(LinkList*)malloc(sizeof(LinkList));
	head->next =end;
	end->next=NULL;
	while(i<=n)
	{
		node=(LinkList*)malloc(sizeof(LinkList));
		scanf("%d %s",&t,&w);
		if(t==1);
		
	}
}
