#include<stdio.h>
int main()
{
	int m,n;
	int front,rear;
	Status InitQueue(sqQueue&Q);
	q.base=(QElemType*) malloc(MXQSIZE *sizeof(QElemType));
	if(!Q.base) exit(OVERFLOW);
    Q.front=Q.rear=0;
    return OK;
}

