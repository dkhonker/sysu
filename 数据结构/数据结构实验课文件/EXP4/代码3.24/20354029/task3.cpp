#include<stdio.h>
int main()
{char a[20001];
 char b[20001];
 int pos;
 int len;
 scanf("%d%d",&pos,&len);
 int i=pos-1;
 gets(a);
 for(int j=0;j<len;i++,j++)
    {b[j]=a[pos-1];}
puts(b);
return 0;
}
