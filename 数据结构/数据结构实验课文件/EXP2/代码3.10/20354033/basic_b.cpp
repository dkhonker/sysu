#include <stdio.h>

const int maxn = 10000;

int m;  //命令数 
int n;  //线性表中元素个数 
int a,i,x;
int main()
{
	scanf("%d",&m);
	int p;
	for(p=1;p<=m;p++)
	scanf("%d %d %d",&a,&i,&x);
	if (a==1)
    void insert(int i,int x);
	else 
	return 0;
	
	
	
}
	
}
	void insert(int i,int x)
	{
	scanf("%d",&x);
	arr[maxn]={x};
	int n=*N;
    if((i<1)||(i>n+1)) return 0;
    else{int j;
    for(j=n;j>=i;j--) L[j]=L[j-1];
    arr[j]=x;*N=++n;return 1;
}
}
void deleteByIndex(int i){ //删除a[i] 
int n=N*;
if((i<1)||(i>n) return 0;
else{int j;
e=arr[i-1;
for(j=i;j<n;j++) arr[j-1]=l[j];
*N=-n;return 1;

}
}
void find(int x){//查找第一个x 
} 
